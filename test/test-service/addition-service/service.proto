syntax = "proto3";

option java_multiple_files = true;

service Snappi {
  rpc Add (AddRequest) returns (AddResponse) {}
}

message AddRequest {
    int32 first = 1;
    int32 second = 2;
}

message AddResponse {
    int32 output = 1;
}
