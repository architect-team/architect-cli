services:
  examples/react-app:latest:
    debug:
      path: ./frontend
    interfaces:
      main:
        subdomain: app
    parameters:
      API_PORT: 8080
  examples/react-app-backend:latest:
    debug:
      path: ./backend
    interfaces:
      main:
        subdomain: api
