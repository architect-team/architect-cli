services:
  examples/react-app:latest:
    debug:
      path: ./frontend
    ingress:
      subdomain: app
  examples/react-app-backend:latest:
    debug:
      path: ./backend
    ingress:
      subdomain: api
