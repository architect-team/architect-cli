services:
  examples/react-app:latest:
    debug:
      path: ./frontend
    interfaces:
      main:
        subdomain: app
  examples/react-app-backend:latest:
    debug:
      path: ./backend
    interfaces:
      main:
        subdomain: api
