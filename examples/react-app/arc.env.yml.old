services:
  examples/react-app:latest:
    debug:
      path: ./frontend/architect.yml.old
    interfaces:
      main:
        subdomain: app
  examples/react-app-backend:latest:
    debug:
      path: ./backend/architect.yml.old
    interfaces:
      main:
        subdomain: api
