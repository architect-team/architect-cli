services:
  examples/database-seeding:latest:
    debug:
      path: .
    interfaces:
      main:
        subdomain: app
    parameters:
      AUTO_DDL: seed ## this might be changed to 'migrate' or 'none' for other environments
