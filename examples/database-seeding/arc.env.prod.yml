services:
  examples/database-seeding:latest:
    debug:
      path: .
    ingress:
      subdomain: api
    parameters:
      AUTO_DDL: none
    dependencies:
      my-demo-db:
        interfaces:
          postgres:
            host: host.docker.internal
            port: 5432
