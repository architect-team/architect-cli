import { ComponentConfig } from './component-config';
import { ComponentSpec } from './component-spec';

export const transformComponent = (spec: ComponentSpec): ComponentConfig => {
  throw new Error('not yet implemented');
};
