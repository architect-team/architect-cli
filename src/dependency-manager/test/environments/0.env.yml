services:
  architect-ci/hello-world:latest:
    interfaces:
      main:
        subdomain:
          hello
  architect-ci/external-image:latest:
    interfaces:
      main:
        subdomain:
          external
  architect-ci/database-seeding:latest:
    interfaces:
      main:
        subdomain: database-seeding
    parameters:
      AUTO_DDL: seed ## this might be changed to 'migrate' or 'none' for other environments
